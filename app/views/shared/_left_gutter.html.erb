<%# content_for :left_gutter do %>
  <%#= to_html(content[:html]) if content[:html] %>
  <%#= wrap(content[:text], (content[:tag] || "p")) if content[:text] %>
<%# end %>

<div class='row'>
  <div class='col-xs-12'>
    <%= render partial: "calendar/calendar", locals:{date: @date} %>
  </div>
</div>
<div class='row'>
  <div class='col-xs-12'>
    <ul class='list-group' >
    <% @active_tasks.each do |active_task| %>
      <%= render partial: "tasks/active_task_li", locals:{active_task:active_task} %>
    <% end %>
    </ul>
  </div>
</div>