<% content_for :left_gutter do %>
  <%#= to_html(content[:html]) if content[:html] %>
  <%#= wrap(content[:text], (content[:tag] || "p")) if content[:text] %>
  <div class='row'>
    <div class='col-xs-12'>
      <%= render partial: "calendar/calendar" %>
    </div>
  </div>
<% end %>